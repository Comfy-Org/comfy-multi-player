# Invariant exceptions

Deliberate deviations from `docs/INVARIANTS.md` require explicit maintainer review before implementation. Add one row per approved deviation; do not use this log to retroactively excuse a violation. Rows marked **PROPOSED** were written by a reviewer and are NOT yet agreed: their owner and sunset date are the maintainer's to assign, and until they are, the row records the deviation without claiming it was signed off.

| Date | Invariant ID | Why | Scope | Expiry |
| --- | --- | --- | --- | --- |
| YYYY-MM-DD | KA-0 / FC-0 | Decision and approver | Exact files, release, or deployment | Date or removal condition |
| ~~2026-08-21 (PROPOSED)~~ **RESOLVED 2026-08-21** | KA-4 | ~~Four `connect` rejections mutate before validating (issue #10).~~ **Sunset condition met: PR #34 landed.** The block asserting the bug went red exactly as designed, the four rows moved into `CASES` in `test/ka4-rejection-byte-identity.test.ts`, and `KNOWN_KA4_VIOLATIONS` is now empty. Struck rather than deleted so the exception's full life — proposed, measured, sunset on its own stated condition — stays legible. | (was `src/applier.ts` `connect` paths) | Expired. Residual KA-4 *ordering* holes are tracked as issues (#59, #61, #68); the deliberate *convergence* carve-outs A6 introduces are the row below, because they are permanent by design rather than bugs awaiting a fix. |
| 2026-08-21 (PROPOSED) | KA-4 | **Four permanent convergence carve-outs**, schema §2.5 items 4-7 / Amendment A6. A `connect`, `set_widget` or `add_node` rejected by a check that must READ a node resolves differently by arrival order when it races that node's deletion (or, for `add_node`, a rival same-`node_id` op), because the check necessarily sits below an early return that consumes the `op_id`; under §4 abort-remainder that reaches the projection. Every OP-ONLY precondition is hoisted above those returns and does NOT carve out. Measured in both orders with converging controls. | `src/applier.ts` `applyConnect` / `applySetWidget` / `applyAddNode`; schema §2.5 items 4-7 | **No sunset — deliberate.** A6 rejects three alternatives with reasons; closing any of them either re-opens issue #10 or changes the concrete-input register's semantics, which is a vocabulary change needing a comfy-cli counterpart. Each item is pinned by a still-diverges test that reddens the day it is closed. Revisit only via a new amendment. |
| 2026-08-21 (PROPOSED) | KA-3 | This applier REJECTS an interior write into a shared subgraph definition where comfy-cli FORKS it, and it counts instances by resolved definition where comfy-cli's `engine._count_instances` matches the literal id string. Pre-declared by schema §5.3, which keeps forking OPEN; recorded here so the divergence has an owner and a sunset rather than living only in source comments. | `src/doc.ts` `countDefinitionInstances`/`definitionAliases`; `src/applier.ts` §5.3 guard | Owner: UNASSIGNED. Sunset PROPOSED, not agreed: the schema §5.3 forking amendment lands and both implementations adopt it. |
